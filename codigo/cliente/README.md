# Código do Cliente (Front-end)

Esta pasta contém o código-fonte do lado cliente da solução.

> **Nota:** O desenvolvimento de front-end depende da realidade da comunidade parceira. Caso o grupo tenha avaliado que apenas os scripts já são uma interface amigável para a realidade da comunidade, justifique a decisão no arquivo `etapas/etapa3-cliente.md`.

## Organização

### Front-end Web (HTML + CSS + JS)

```cmd
cliente/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── app.js
├── assets/
│   └── (imagens e recursos)
└── README.md
```

### CLI / Scripts de Cliente

```cmd
cliente/
├── scripts/
│   └── (scripts de uso pelo cliente)
└── README.md
```

## Como Executar

*(Descreva aqui como executar o cliente localmente para desenvolvimento e testes.)*

## Dependências

*(Liste as dependências do front-end e como instalá-las.)*
